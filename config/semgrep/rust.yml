rules:
  - id: rust-shell-command-construction
    message: Dynamic text is passed to a shell command interpreter.
    languages: [rust]
    severity: ERROR
    patterns:
      - pattern-either:
          - pattern: std::process::Command::new("sh").arg("-c").arg($CMD)
          - pattern: Command::new("sh").arg("-c").arg($CMD)
