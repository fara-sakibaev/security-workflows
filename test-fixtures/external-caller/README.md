# External caller fixture

Copy this directory into a separate GitHub repository. Replace `OWNER` and `FULL_COMMIT_SHA`, then run both workflows. Relative workflow references do not constitute this test.
